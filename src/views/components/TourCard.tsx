'use client';
import React, { useState } from 'react';
import { Bus, CheckCircle2, ChevronRight, MapPin, MessageCircle } from 'lucide-react';
interface TourProps { title: string; category: string; transportType: string; priceVeg: number; availableSeats: number; totalSeats?: number; slug: string; pickupLocation?: string; inclusions?: string[]; }
export const TourCard: React.FC<{ tour: TourProps }> = ({ tour }) => { const [showItinerary, setShowItinerary] = useState(false); const total = tour.totalSeats || 30; const progressPercent = Math.round(((total - tour.availableSeats) / total) * 100); const whatsappBookingUrl = `https://wa.me/919830072946?text=${encodeURIComponent(`Hi Chuti Chuti Tours, I want to reserve a seat for the ${tour.title}`)}`; const inclusions = tour.inclusions || ['Sovabazar Rajbari Grand Lunch', 'Refreshments & Breakfast Packet', 'Traditional Bengali Bhog']; return <div className="group bg-white rounded-3xl border border-[#0D223A]/10 shadow-sm hover:shadow-2xl transition-all flex flex-col justify-between overflow-hidden"><div className="p-6 pb-2"><div className="flex justify-between items-center mb-3"><span className="text-[11px] font-bold uppercase tracking-wider text-[#D49A3D] bg-[#0D223A] px-3 py-1 rounded-full">{tour.category}</span><span className="text-xs font-semibold text-rose-600 bg-rose-50 px-2.5 py-0.5 rounded-full">Limited Seats</span></div><h3 className="text-2xl font-serif font-bold text-[#0D223A] leading-snug">{tour.title}</h3><div className="mt-4 space-y-2 text-xs text-[#0D223A]/80 font-medium border-t border-b border-[#0D223A]/5 py-3"><p className="flex items-center gap-2"><Bus className="w-4 h-4 text-[#D49A3D]" /><span><strong>Transport:</strong> {tour.transportType}</span></p><p className="flex items-center gap-2"><MapPin className="w-4 h-4 text-[#D49A3D]" /><span><strong>Pickup:</strong> {tour.pickupLocation || 'Esplanade, Kolkata (8:00 AM)'}</span></p></div><div className="mt-4"><div className="flex justify-between text-[11px] font-semibold text-[#0D223A]/70 mb-1"><span>Seats Reserved</span><span className="text-[#0D223A] font-bold">{tour.availableSeats} Left</span></div><div className="w-full h-2 bg-gray-100 rounded-full overflow-hidden"><div className="h-full bg-gradient-to-r from-[#D49A3D] to-amber-600 rounded-full" style={{ width: `${progressPercent}%` }} /></div></div><button onClick={() => setShowItinerary(!showItinerary)} className="mt-4 w-full text-left text-xs font-bold text-[#0D223A] flex items-center justify-between py-2 border-t border-[#0D223A]/5"><span>{showItinerary ? 'Hide Inclusion Details' : 'View Key Inclusions'}</span><ChevronRight className={`w-4 h-4 transition-transform ${showItinerary ? 'rotate-90' : ''}`} /></button>{showItinerary && <ul className="mt-2 text-xs space-y-1.5 text-[#0D223A]/80 bg-[#F9F5EC] p-3 rounded-xl border border-[#0D223A]/10">{inclusions.map((inclusion) => <li key={inclusion} className="flex items-center gap-1.5"><CheckCircle2 className="w-3.5 h-3.5 text-emerald-600" />{inclusion}</li>)}</ul>}</div><div className="bg-[#F9F5EC] p-6 pt-4 border-t border-[#0D223A]/10 flex items-center justify-between gap-4"><div><span className="text-[10px] uppercase font-bold text-[#0D223A]/60 block">Starting at</span><span className="text-2xl font-serif font-extrabold text-[#0D223A]">₹{tour.priceVeg}</span><span className="text-[10px] text-[#0D223A]/60 font-semibold"> / seat</span></div><a href={whatsappBookingUrl} target="_blank" rel="noopener noreferrer" className="flex-1 bg-[#0D223A] hover:bg-[#D49A3D] text-[#F9F5EC] hover:text-[#0D223A] font-extrabold py-3 px-4 rounded-xl text-xs transition-all flex items-center justify-center gap-2 text-center"><MessageCircle className="w-4 h-4" /><span>Book via WhatsApp</span></a></div></div>; };
